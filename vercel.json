{
  "github": {
    "silent": true
  },
  "installCommand": "curl -fsSL https://bun.sh/install | bash && $HOME/.bun/bin/bun install --frozen-lockfile",
  "buildCommand": "$HOME/.bun/bin/bun run docs:build",
  "outputDirectory": "docs/.vitepress/dist"
}
